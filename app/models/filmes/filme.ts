class Filme {
  capa: string;
  titulo: string;
  descricao: string;

  constructor(capa: string, titulo: string, descricao: string) {
    this.capa = capa;
    this.titulo = titulo;
    this.descricao = descricao;
  }
}
